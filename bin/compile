#!/usr/bin/env bash

# fail fast
set -eu

# parse args
BP_DIR="$(cd "$(dirname "$0")" && pwd)"
BUILD_DIR=$1
OPT_DIR=$BP_DIR/../opt/

heroku_bin_dir=$BUILD_DIR/.heroku/bin
mkdir -p $heroku_bin_dir
cp $OPT_DIR/* $heroku_bin_dir/
chmod +x ${heroku_bin_dir}/recompose
chmod +x ${heroku_bin_dir}/web
